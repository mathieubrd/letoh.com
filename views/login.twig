{% extends 'base.twig' %}

{% block content %}
    <section class="section">
        <h2>Se connecter</h2>
        <p>
            Veuillez entrez vos informations de connexion pour pouvoir réserver une chambre d'hotel sur notre site.<br />
            Si vous ne possédez pas de compte, cliquez <a href="{{ app.url_generator.generate('signup') }}">ici</a> pour en créer un.
        </p>

        <form action="{{ path('account_login_check') }}" method="post">
            <div class="form__field">
                <span class="form__field__name">Adresse e-mail</span>
                <input type="text" class="input-default" name="_username" />
                <div class="form__field__error">{{ error }}</div>
            </div>
            <div class="form__field">
                <span class="form__field__name">Mot de passe</span>
                <input type="password" class="input-default" name="_password" value="{{ last_username }}" />
            </div>
            <input type="submit" class="btn btn--blue form__submit" />
        </form>
    </section>
{% endblock %}